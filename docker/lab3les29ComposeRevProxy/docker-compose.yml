version: "3"

services:
  nginx:
    image: nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf

  app:
    image: python:3.11-slim
    command: ["python", "-m", "http.server", "9000"]
